1) Create a new branch conflict-branch.
-  git checkout -b "conflict-branch"

2) Modify the same line in intro.txt in 
both main and conflict-branch 
- i) modify into same line in conflict-branch and add and commit it.
 ii) modify into same line in git-learning , add and commit it. 

3) Try to merge and observe the conflict 
- if i try to merge it by command "git merge conflict-branch" give me merge conflict. 

4) Resolve the conflict manually. 
- resolve it manually in the editor 
 
5) Complete the merge.
- thenn complete the merge , then commit .
